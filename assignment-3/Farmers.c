#include <stdio.h>
#include <string.h>

int main()
{
    



      /*
    int t;
    scanf("%d", &t);

    int s[t][3];    

    for (int i = 0; i < t; i++)
    {
        for (int j = 0; j < 3; j++)
        {
            scanf("%d", &s[i][j]);
        }
    }

    
  

    for (int i = 0; i < t; i++)
    {

        int days = (s[i][0] * s[i][2]) / (s[i][0] + s[i][1]);
        int dif = s[i][2] - days;

        if (dif < 0)
        {
            printf("%d\n", 1);
        }
        else {
            printf("%d\n", dif);
        }
    }

    */

    return 0;
}